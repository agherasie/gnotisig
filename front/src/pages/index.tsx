import { VStack } from "@chakra-ui/react";
import { FC } from "react";
import ConnectButton from "../components/Connect";

const Home: FC = () => (
  <VStack>
    <ConnectButton />
  </VStack>
);

export default Home;
